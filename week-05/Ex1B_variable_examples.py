# Lab 1 
customer_id = 12345
customer_name = 'Jhon Smith'
customer_gender = 'male'
customer_date_of_birth = '01/01/1990'
drivers_license_number = 'D1234567'
auto_policy_number = 'P1234567'

customer_id = 12345  # assumed numeric customer ID; could also be a string if it includes letters
customer_name = 'John Smith'  # assumed full customer name in one variable; could use first_name and last_name separately
customer_gender = 'male'  # assumed gender as text; real systems may use a small set of allowed values
customer_date_of_birth = '01/01/1990'  # assumed date stored as a string; better formats are YYYY-MM-DD or a date object
drivers_license_number = 'D1234567'  # assumed license number is an alphanumeric string
auto_policy_number = 'P1234567'  # assumed policy number is stored as a string, not a numeric value

customer_id = 12345  # Assumed numeric ID; could be string if it has letters or leading zeros
customer_name = 'Jhon Smith'  # Full name as one string; might need first_name and last_name separately
customer_gender = 'male'  # Text value; real apps often use codes like 'M' or 'F'
customer_date_of_birth = '01/01/1990'  # Date as string; better as datetime object for calculations
drivers_license_number = 'D1234567'  # Alphanumeric string; may need validation for format
auto_policy_number = 'P1234567'  # String to preserve leading zeros; could be numeric if no letters

# 4
name = 'Samia'
city = 'Asmara'
state = 'Maekel'

# Lab 2
# a) Full list of Python reserved words that cannot be used as variable names:
# False, None, True, and, as, assert, break, class, continue,
# def, del, elif, else, except, finally, for, from, global,
# if, import, in, is, lambda, nonlocal, not, or, pass,
# raise, return, try, while, with, yield

# b) Definitions of 5 Python reserved words:

# if - Used to make a decision in a program. It runs code only if a condition is True.

# for - Used to create a loop that repeats code for each item in a sequence.

# import - Used to bring a module or library into a Python program.

# class - Used to create a ^^blueprint^^ for objects in object-oriented programming.

# lambda - Used to create a small anonymous function with a single expression.

